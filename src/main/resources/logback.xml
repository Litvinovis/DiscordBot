<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="MAIN-LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>main-log.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>main-log.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- each archived file, size max 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- total size of all archive files, if total size > 20GB, it will delete old archived file -->
            <totalSizeCap>100MB</totalSizeCap>
            <!-- 60 days to keep -->
            <maxHistory>2</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern> %d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="MAIN-LOG" />
    </root>

    <logger name="default-logger" level="DEBUG" additivity="false">
        <appender-ref ref="MAIN-LOG"/>
    </logger>

</configuration>